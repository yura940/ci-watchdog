name: 🕵️‍♂️ Gitleaks Secret Scan

on:
  push:
    branches: 
      - main
  pull_request:
    branches: 
      - main

jobs:
  gitleaks-scan:
    name: Run Gitleaks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Gitleaks to detect secrets
        uses: zricethezav

